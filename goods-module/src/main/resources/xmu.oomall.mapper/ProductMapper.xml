<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xmu.oomall.mapper.ProductMapper">

    <select id="findProductById" resultType="product" parameterType="Integer">
        select * from oomall_product where id = #{id}
    </select>

    <select id="findProductList" resultType="product" >
        select * from oomall_product
        where goods_id like #{goodsId}
    </select>

    <insert id="addProduct" parameterType="product" useGeneratedKeys="true" keyProperty="id">
        insert into oomall_product(pic_Url, specifications, goods_id, price, gmt_create, gmt_modified, is_deleted)
        values ( #{picUrl}, #{specifications}, #{goodsId}, #{price}, #{gmtCreate}, #{gmtModified}, #{beDeleted})
    </insert>

    <update id="updateProduct" parameterType="product">
        update oomall_product
        set pic_url = #{picUrl},
            specifications = #{specifications},
            goods_id = #{goodsId},
            price = #{price},
            gmt_create = #{gmtCreate},
            gmt_modified = #{gmtModified},
            is_deleted = #{beDeleted}
        where id = #{id}
    </update>

    <delete id="deleteProductById" parameterType="Integer">
        delete from oomall_product
        where id = #{id}
    </delete>
</mapper>